services:
  - type: web
    name: saryx-relay
    env: node
    plan: free
    rootDir: relay-server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
